import * as React from "react";
import { hot } from "react-hot-loader";
import { Heading } from "../../components/Typography";

@hot(module)
export class Faculties extends React.Component {
  render() {
    return (
      <>
        <Heading>Факультеты</Heading>
      </>
    );
  }
}
